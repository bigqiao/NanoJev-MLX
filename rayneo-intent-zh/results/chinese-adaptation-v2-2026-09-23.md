# 2026-09-23 NanoJev 中文意图后续实验

保持原定 0.85 阈值、相同 240 条训练集和 80 条开发集，将原生 MLX QLoRA 从 240 步延长到 400 步。只在开发集上按 BCE 选中第 360 步，然后冻结编码器训练原有 scalar 分类头；按开发集 BCE 选中第 105 epoch。训练过程没有读取原 64 条测试集或 52 条回归集，也没有改动 NanoJev 原仓库。候选保存在本项目忽略的 `models/nanojev-intent-zh-lora-head-v2`，权重 SHA256 为 `4032c670cab8319130ec148beef14386aa518b09cc5196c08ef379cc5180726c`。

模型选择在 52 条回归之前写入 [选择记录](lora-head-v2-selection-before-regression.json)：相同阈值下，真实 API 开发集的两个意图均增加 TP 且不增加 FP。52 条已评测过 v1，**因此这里只是回归，不是新的盲测**。本轮没有按它的分数调整权重、阈值或提示词。

| 集合与意图 | v1 TP / FP / FN / TN | v2 TP / FP / FN / TN | v1 → v2 召回 |
| --- | --- | --- | --- |
| 开发集 80：求助 | 8 / 2 / 16 / 54 | 14 / 1 / 10 / 55 | 33.3% → 58.3% |
| 开发集 80：日程 | 13 / 2 / 11 / 54 | 18 / 0 / 6 / 56 | 54.2% → 75.0% |
| 回归集 52：求助 | 5 / 0 / 7 / 40 | 6 / 0 / 6 / 40 | 41.7% → 50.0% |
| 回归集 52：日程 | 4 / 0 / 10 / 38 | 8 / 0 / 6 / 38 | 28.6% → 57.1% |

以上为两问分别统计，同一案例可能两问都为正例。v2 在 52 条上的求助改善只有 1 条，仍漏掉一半正例；38/40 个负例上的零误报也不能推断日常连续录音时的误触发率。**主动提示默认继续关闭**，明确求助继续走原有直接入口。当前运行的 8765 服务未被替换；v2 用独立的 127.0.0.1:8766 端口核对后已关闭。启动器默认模型仍为原版，v2 只是可明确指定的实验权重。

证据：

- [400 步 QLoRA](nanojev-intent-zh-lora-v2-training.json)、[v2 预检](nanojev-intent-zh-lora-v2-preflight.json)和 [v2 分类头](nanojev-intent-zh-lora-head-v2-training.json)记录 train/dev 数量、数据与脚本 SHA256、训练曲线以及选择点。开发集 BCE 分别为 0.338155（QLoRA）与 0.128102（分类头）。训练时的脚本快照在忽略的模型目录内；之后的源码仅改了预检文件命名，避免不同轮次互相覆盖。
- [逐张量导出检查](lora-head-v2-export-verification.json)：v2 编码器到 v2+分类头的 322 项中仅 `scalar.weight`、`scalar.bias` 改变，其余 320 项字节一致。
- v2 开发集真实 `/api/evaluate` 的 80 条 × 2 轮输出在 `.runtime/intent/dev80-lora-head-v2-api.json`；分数两轮完全一致。旧模型基线也在本轮以当前 8765 实例重新执行并存于 `.runtime/intent/dev80-baseline-live.json`，得到旧记录相同的 0.85 指标。
- [v2 52 条回归](holdout52-lora-head-v2-2026-09-23.json)为真实 MLX API 输出，52 条 × 2 轮分数完全一致，API 报告远程模型调用为 0。完整原始返回、模型身份、哈希与逐条分数保存在 JSON。批次 HTTP 中位延迟约 470 ms，不是单句或眼镜端到端延迟。

下一轮需按完整会话隔离真实 ASR 片段，独立人工标注明确求助、普通疑问、本人已确定安排、他人事项、取消、更正及断句错误；将数据分成训练、校准和全新盲测。用校准集事先确定允许的误报和漏报，再做全新盲测及持续录音观察。原 64 条和 52 条只能继续作回归。
